<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#000"
    android:padding="12dp">

    <TextView android:text="Checkout" android:textSize="18sp" android:textColor="#fff" android:textStyle="bold"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/checkout_recycler"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="12dp" />

    <LinearLayout android:layout_width="match_parent" android:layout_height="wrap_content" android:orientation="horizontal" android:gravity="center_vertical">
        <TextView android:text="Total: " android:textColor="#aaa" android:layout_marginEnd="8dp"/>
        <TextView android:id="@+id/total_amount" android:textColor="#fff" android:textStyle="bold" />
    </LinearLayout>

    <Button android:id="@+id/btn_continue_payment" android:layout_width="match_parent" android:layout_height="wrap_content" android:text="Continue Payment" android:backgroundTint="#E11A1A" android:textColor="#fff" android:layout_marginTop="12dp"/>
</LinearLayout>